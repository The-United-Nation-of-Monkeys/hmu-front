# SmartGrant Frontend

Полнофункциональный фронтенд для системы управления грантами SmartGrant, построенный на React + TypeScript + Vite.

## 🚀 Технологии

- **React 18** + **TypeScript**
- **Vite** - быстрая сборка и разработка
- **TailwindCSS** + **shadcn/ui** - современный UI
- **React Router** - маршрутизация
- **Zustand** - управление состоянием (authStore, uiStore, grantStore)
- **React Query** - управление серверным состоянием
- **Axios** - HTTP клиент
- **i18next** - интернационализация (en / es / ru)
- **Lucide React** - иконки

## 📁 Структура проекта

```
src/
├── api/              # API клиенты (устаревшее, используйте lib/)
├── components/       # React компоненты
│   ├── ui/          # shadcn/ui компоненты
│   ├── gov/         # Компоненты для Government
│   └── grantee/     # Компоненты для Grantee
├── hooks/           # Custom React hooks
├── i18n/            # Переводы (en, ru, es)
├── layouts/         # Layout компоненты
├── lib/             # API клиенты и утилиты
├── pages/           # Страницы приложения
│   ├── gov/        # Government страницы
│   ├── uni/        # University страницы
│   └── grantee/    # Grantee страницы
└── stores/         # Zustand stores
```

## 🎯 Роли и функционал

### Government (Правительство)
- ✅ Создание грантов
- ✅ Просмотр всех грантов
- ✅ Просмотр транзакций
- ✅ Просмотр логов смарт-контракта

### University (Университет)
- ✅ Просмотр грантов
- ✅ Просмотр запросов на транши
- ✅ Подтверждение top-3 запросов
- ✅ Просмотр AML-флагов
- ✅ Просмотр логов смарт-контракта

### Grantee (Получатель)
- ✅ Просмотр своих грантов
- ✅ Создание мета-пунктов расходов
- ✅ Заявка на транш (spending request)
- ✅ Загрузка чеков
- ✅ Просмотр статусов и AML-флагов
- ✅ Просмотр истории транзакций и логов

## 🛠 Установка и запуск

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка окружения

Скопируйте `.env.example` в `.env`:

```bash
cp .env.example .env
```

Отредактируйте `.env`:

```env
VITE_API_URL=http://localhost:8000
VITE_APP_NAME=SmartGrant
```

### 3. Запуск dev сервера

```bash
npm run dev
```

Приложение будет доступно по адресу: `http://localhost:3000`

### 4. Сборка для production

```bash
npm run build
```

Собранные файлы будут в папке `dist/`

### 5. Просмотр production сборки

```bash
npm run preview
```

## 📚 API клиенты

Все API клиенты находятся в `src/lib/`:

- `api.ts` - базовый Axios instance
- `auth.ts` - аутентификация
- `grants.ts` - работа с грантами
- `spending.ts` - spending items и requests
- `receipts.ts` - загрузка чеков
- `contract.ts` - логи смарт-контракта
- `aml.ts` - AML флаги

## 🎨 Компоненты UI

Все shadcn/ui компоненты находятся в `src/components/ui/`:

- Button, Card, Input, Select
- Table, Tabs, Dialog, Drawer, Sheet
- Badge, Alert, Skeleton, Tooltip, Toast

Кастомные компоненты:

- `DataTable` - универсальная таблица данных
- `FileUpload` - загрузка файлов с drag & drop
- `StatusBadge` - бейдж статуса запроса
- `AmountBadge` - отображение суммы
- `AmlFlagList` - список AML флагов
- `ContractLogList` - список логов контракта
- `LanguageSwitcher` - переключатель языка

## 🌍 Интернационализация

Поддерживаемые языки:
- 🇺🇸 English (en)
- 🇷🇺 Русский (ru)
- 🇪🇸 Español (es)

Переводы находятся в `src/i18n/locales/`.

## 🔐 Аутентификация

Аутентификация реализована через Zustand store (`authStore`):

```typescript
import { authStore } from '@/stores/authStore'

const { user, token, isAuthenticated, login, logout } = authStore()
```

Токен автоматически добавляется в заголовки всех API запросов через Axios interceptor.

## 🛡 Защищенные маршруты

Используется компонент `ProtectedRoute` для защиты маршрутов:

```tsx
<ProtectedRoute allowedRoles={['government']}>
  <DashboardLayout />
</ProtectedRoute>
```

## 📝 Примеры использования

### Создание запроса с React Query

```typescript
import { useMutation, useQueryClient } from '@tanstack/react-query'
import { grantsApi } from '@/lib/grants'

const mutation = useMutation({
  mutationFn: grantsApi.createGrant,
  onSuccess: () => {
    queryClient.invalidateQueries({ queryKey: ['grants'] })
  },
})

mutation.mutate({ title: 'New Grant', ... })
```

### Использование Toast

```typescript
import { useToast } from '@/components/ui/use-toast'

const { toast } = useToast()

toast({
  title: 'Success',
  description: 'Grant created successfully',
})
```

### Использование i18n

```typescript
import { useTranslation } from 'react-i18next'

const { t } = useTranslation()

<h1>{t('dashboard')}</h1>
```

## 🧪 Тестирование

Для запуска линтера:

```bash
npm run lint
```

## 📦 Зависимости

Основные зависимости:

- `react` & `react-dom` - UI библиотека
- `react-router-dom` - маршрутизация
- `zustand` - state management
- `@tanstack/react-query` - server state
- `axios` - HTTP клиент
- `i18next` & `react-i18next` - интернационализация
- `tailwindcss` - CSS framework
- `lucide-react` - иконки

Полный список в `package.json`.

## 🔗 Интеграция с Backend

Frontend ожидает FastAPI backend на `http://localhost:8000` (или значение из `VITE_API_URL`).

Все API endpoints должны соответствовать структуре, описанной в `src/lib/`.

## 📄 Лицензия

MIT

## 👥 Разработка

Для разработки рекомендуется:

1. Использовать TypeScript строго
2. Следовать структуре проекта
3. Использовать существующие компоненты UI
4. Добавлять переводы для всех текстов
5. Тестировать на всех трех ролях

## 🐛 Известные проблемы

- Некоторые компоненты могут требовать дополнительной настройки для production
- File upload требует настройки CORS на backend

## 📞 Поддержка

При возникновении проблем проверьте:

1. Правильность настройки `.env`
2. Запущен ли backend сервер
3. Консоль браузера на наличие ошибок
4. Network tab для проверки API запросов

